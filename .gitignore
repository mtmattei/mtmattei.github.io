# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# working/source files (kept locally, not versioned):
# raw phone/screen captures. Anything the site actually serves lives under
# public/ and must be exempted here, or it ships to dist locally, passes
# every build, and 404s in production - which is exactly what happened to
# public/projects/misregistration.mp4.
*.mp4
!public/**/*.mp4
# the landing mockup working dir (has its own .git) and loose session artifacts
mockup-landing/
272d0759-96b2-48c1-ad3f-96b92033e83b.png
fibonacci-sphere-demo.png
